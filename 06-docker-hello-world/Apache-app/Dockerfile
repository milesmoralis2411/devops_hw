# Apache (httpd) Hello World
FROM httpd:2.4

# httpd serves files from /usr/local/apache2/htdocs/
COPY index.html /usr/local/apache2/htdocs/index.html

EXPOSE 80
# The httpd base image already starts the server via its default CMD
