# Nginx Hello World
FROM nginx:alpine

# nginx serves files from /usr/share/nginx/html/
COPY index.html /usr/share/nginx/html/index.html

EXPOSE 80
# The nginx base image already starts the server via its default CMD
